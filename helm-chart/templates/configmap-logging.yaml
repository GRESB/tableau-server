{{- if .Values.tableauServer.fluentd.enable -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "tableau-server.fluentdName" . }}
data:
  {{- toYaml .Values.tableauServer.fluentd.config | nindent 2 }}
{{- end }}
